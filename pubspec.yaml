name: flutter_application_1
description: "Porter - Multi-role food delivery app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  google_sign_in: ^6.2.2
  cloud_firestore: ^5.6.5
  firebase_storage: ^12.4.4
  firebase_messaging: ^15.2.4

  # Maps
  google_maps_flutter: ^2.10.0
  google_maps_flutter_web: ^0.5.10
  geolocator: ^13.0.2
  http: ^1.2.2

  # State Management
  provider: ^6.1.2

  # Routing
  go_router: ^14.8.1

  # UI
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.4.1
  flutter_rating_bar: ^4.0.1
  shimmer: ^3.0.0
  badges: ^3.1.2
  intl: ^0.20.2

  # Utilities
  uuid: ^4.5.1
  image_picker: ^1.1.2
  url_launcher: ^6.3.1
  flutter_colorpicker: ^1.1.0
  image: ^4.8.0
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  fl_chart: ^1.1.1
  flutter_local_notifications: ^21.0.0
  google_sign_in_all_platforms: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/

flutter_launcher_icons:
  android: true
  ios: true
  windows: true
  image_path: "assets/icon/porter_icon.png"
  min_sdk_android: 21
  adaptive_icon_background: "#C02525" # Close to the red color in the logo
  adaptive_icon_foreground: "assets/icon/porter_icon.png"
